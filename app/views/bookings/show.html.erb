</div>
<div class="banner banner-small" style="background-image: linear-gradient(-225deg, rgba(0,101,168,0.6) 0%, rgba(0,36,61,0.6) 50%), url('<%= @gig.photos.first.photo if @gig.photos.present? %>');">
  <div class="container">
  <div class="row">
    <div class="banner-content">
      <p><%= @gig.date.strftime('%A, %d %b %Y') + ' @ ' + @gig.time.strftime('%l:%M %p') %></p>
      <h1> <span class="<%= @booking.status=="confirmed" ? 'booking-status-confirmed' : 'booking-status-pending' %>"><%= @booking.status.capitalize + ' Booking:' %></span> <%=  @gig.name %> </h1>
      <h2>Organized by <%= @gig.organizer.name %>
      </h2>
    </div>
  </div>
    <div class="row">
    <div class="col-sm-12 text-center">
      <div class="col-sm-4 col-sm-offset-4 text-center" style="margin-top: 20px;">
        <%= link_to "View gig", gig_path(@gig), class: "btn btn-primary banner-link", 'data-aos': "zoom-in", 'data-aos-duration': 100 %>
      </div>
    </div>
  </div>
</div>
</div>
</div>

<div class="container" style="color: white; opacity: 0.9;">
  <div class="row show-text">
    <div class="col-sm-12 col-md-8 col-md-offset-2">
      <h2>Description</h2>
      <p><%= @gig.description %></p>
    </div>
    </div>
  </div>
</div>

<div
  id="map"
  style="width: 100%;
  height: 500px;
  box-shadow: -1px -6px 14px 0px #00000078;"
  data-markers="<%= @markers.to_json %>"
  style="border-radius: 3px; box-shadow: 1px 1px 1px rgba(0,0,0,0.1); margin-bottom: 0; margin-top: 20px; padding-bottom: 0;"
></div>
